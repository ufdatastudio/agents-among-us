[project]
name = "agents-among-us"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
]

[project.optional-dependencies]
api = [
    "openai>=1.0.0",
    "anthropic>=0.30.0",
    "python-dotenv>=1.0.0",
]
